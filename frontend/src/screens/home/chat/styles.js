import { makeStyles } from '@material-ui/core'

export const useStyles = makeStyles((theme) => ({
  paper: {
    backgroundColor: '#eee',
    height: '100%',
  },
  cardActions: {
    display: 'flex',
    padding: 5,
    alignItems: 'center',
  },
}))
